@import 'colors';
@import 'font';

#timetable {
    h2 {
        position: relative
        padding-top: 13px
        padding-bottom: 13px
        box-sizing: border-box
        width: 100%
        margin: 0 auto
        color: blue
        border-bottom: 1px solid grey-light
        text-align: center
        background: #FFF
        z-index: 2
    }

    #wrapper {
        position: absolute
        width: 100%
        overflow: hidden
        bottom: 0

        ul {
            position: absolute
            width: 100%

            > li {
                text-align: center
            }
        }
    }
}